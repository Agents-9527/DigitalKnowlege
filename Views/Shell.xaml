<Window x:Class="知识产权数字化平台.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:知识产权数字化平台.Views"
        xmlns:share="clr-namespace:知识产权数字化平台.Share"
        xmlns:prism="http://prismlibrary.com/" 
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStyle="None"
        Background="{DynamicResource LightColor}"
        AllowsTransparency="False"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        mc:Ignorable="d"
        Height="1032" Width="1920">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="60"/>
    </WindowChrome.WindowChrome>
    <Grid WindowChrome.IsHitTestVisibleInChrome="True">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Panel.ZIndex="9999">
            <Button Style="{StaticResource ButtonIconStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Content="&#xe624;"  Click="Button_Min"/>
            <Button Style="{StaticResource ButtonIconStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Content="&#xe622;"  Click="Button_Max"/>
            <Button Style="{StaticResource ButtonIconStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Content="&#xe69b;"  Click="Button_CloseMainWindow"/>
        </StackPanel>
        <ContentControl prism:RegionManager.RegionName="{x:Static share:RegionNames.MainRegion}"/>
    </Grid>

</Window>
